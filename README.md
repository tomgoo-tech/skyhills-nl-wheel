# Skyhills Casino Geluksrad — Champagne

Отдельный яркий красно-золотой royal-концепт с сохранённой механикой исходного
лендинга.

Сборка на основе финальной версии `v5_champagne`: `skyhills-casino`.

## Запуск

### macOS

Дважды щёлкните `start.command` или запустите:

```sh
python3 -m http.server 8080
```

Затем откройте <http://localhost:8080/>.

### Windows

Дважды щёлкните `start.bat`, затем откройте
<http://localhost:8080/>.

## Параметры

- Title: `Skyhills Casino Geluksrad — Champagne`.
- Язык: нидерландский.
- Бонус: `€ 4,000 + 100 FS`.
- Google Ads conversion: `AW-18125271250/4qCBCPSTsdwcENLh5sJD`.
- Трекер: <https://tmhh7.com/LPzj3Kzn>.
- Отдельные адаптивные фоны для desktop и mobile.
- Логотип: `Skyhills Casino` с прозрачным фоном.

## Что переписано

- Окно выигрыша переведено из сплошной красной гаммы в светлую палитру
  champagne gold; красный сохранён только в типографике, лучах и тонком контуре.
- Над подписью `PREMIUM REWARD` добавлена компактная полудуга карточных мастей
  `♠ ♥ ♣ ♦`, связывающая окно выигрыша с тематикой казино.
- Google tag размещён официальным блоком в `<head>`, а событие `conversion`
  отправляется только по кнопке `BONUS CLAIMEN` с `value: 1.0` и `currency: USD`.
- HTML, CSS и JavaScript разделены на отдельные файлы.
- Колесо, металлическая рама, лампы, указатель и центральная кнопка собраны CSS-кодом.
- Лампы работают как бегущая цирковая гирлянда, ускоряются во время вращения
  и получили значительно более яркие круглые ядра с многослойным жёлтым гало.
- Для более плавного вращения диск вынесен в отдельный графический слой, а
  гирлянда теперь меняет только прозрачность без тяжёлой перерисовки свечения.
- Гирлянда анимирует только цвет самих круглых ламп; отдельные слои свечения и
  движущийся блик отключены, чтобы исключить прямоугольные GPU-артефакты.
- Каждая лампа получила ровное статичное двухступенчатое золотое гало; бегущий
  импульс меняет яркость ядра и не перемещает слой свечения.
- Стрелка полностью неподвижна и всегда точно указывает на выигрышный сектор.
- Сектора переведены в насыщенную красно-золотую палитру, а обод получил
  усиленное горячее золотое свечение.
- Центральная кнопка выполнена в ярком глянцевом зелёном цвете с золотой рамой.
- Стеклянная фактура центральной кнопки подготовлена по предоставленному
  референсу отдельным прозрачным PNG; надпись остаётся живым HTML-текстом.
- Текстура центральной кнопки уменьшена до 512 px и предзагружается в `<head>`;
  прозрачные края исходного стеклянного изображения полностью сохранены.
- Все подписи секторов белые, жирные и контрастные. Красные сектора приведены
  к единому рубиновому оттенку `#d40018`.
- Для подписей увеличена высота строки, снята обрезка и смягчена нижняя тень.
- Удалён фиксированный полупрозрачный сектор подсветки, который проявлялся
  серым фантомом во время вращения.
- Значения `100` и `4,000` уменьшены на 20% и используют одинаковый размер шрифта.
- Символы `€` и `FS` имеют размер 50% от связанного числового значения.
- Корона удалена; логотип и нидерландский заголовок опущены ниже и адаптивно
  выстроены над колесом.
- Между логотипом и заголовком, а также между колесом и блоком попыток добавлены
  плавно масштабируемые отступы для desktop и mobile.
- Адаптивный промежуток между заголовком и колесом увеличен.
- Центральная кнопка увеличена, поэтому `DRAAIEN` свободно помещается даже на
  компактных экранах.
- Кнопки `NOG EEN KEER` и `BONUS CLAIMEN` получили ту же зелёную стеклянную
  фактуру, золотую рамку и свечение, что и центральная кнопка.
- Весь интерфейс использует локально встроенный вариативный шрифт Manrope.
- Числовые значения увеличены примерно на 35–40%, подписи секторов — примерно
  на 50%; тяжёлый капс и грубые тени убраны.
- Вращение завершается по событию `animationend` с безопасным fallback-таймером.
- Добавлена блокировка повторных кликов во время вращения.
- Состояние выигрыша сохраняется отдельно для royal-red-концепта.
- Tracking и редирект вынесены в отдельные функции.
- Добавлена поддержка `prefers-reduced-motion`, клавиатуры и safe-area iPhone.
